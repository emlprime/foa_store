It is a spin-off project from Satchmo (configure settings via an admin interface).

Ensure that my_project/settings.py has the following specific configurations (in addition to the defaults and your other app needs):

INSTALLED_APPS = (
	....
	'keyedcache',
	'livesettings',
	....
)

